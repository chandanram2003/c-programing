#include <stdio.h>
int main()
{
    int arr[5];
    for (int i = 0; i < arr[i]; i++)
    {
        int a = i + 1;
        printf("Enter the number %d\n", i);
        scanf("%d", &arr[i]);
    }
    printf("%d", arr[1]);
}